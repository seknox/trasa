class Constants {
  static TRASA_HOSTNAME = `https://${window.location.hostname}`;

  static TRASA_GUAC_HOSTNAME = `https://${window.location.hostname}`;

  static TRASA_GUAC_HOSTNAME_WEBSOCKET = `wss://${window.location.hostname}`;

  static TRASA_HOSTNAME_WEBSOCKET = `wss://${window.location.hostname}`;

  static VERSION = '';

  static ISEE = false;
}
export default Constants; // {}// {TRASA_HOSTNAME:"https://api.trasa.seknox.com"};
