export type AccessStatsFilterProps = {
  timeFilter: string;
  statusFilter: string;
  entityType: string;
  entityID: string;
};

export type ResourceStatsFilterProps = {
  entityType: string;
  entityID: string;
};
